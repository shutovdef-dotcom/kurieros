## Что меняется

<!-- 1–3 предложения о сути изменения -->

## Проверка

- [ ] `npm test` зелёный
- [ ] `npm run typecheck` зелёный
- [ ] `npm run lint` зелёный
- [ ] `npm run build` зелёный
- [ ] Изменения проверены руками в превью (`npm run dev`) на mobile + desktop

## CSS / шаблоны

- [ ] Если добавлены новые `<header>`, `<main>`, `<section>`, `<article>`, `<aside>` или `<nav>` — у каждого есть дискриминирующий класс (см. `kurieros-astro/docs/css-conventions.md`)
- [ ] В `src/styles/*.css` нет новых селекторов по голому HTML-тегу

## Связано

<!-- backlog id / issue / PR -->
