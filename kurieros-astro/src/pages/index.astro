---
import BaseLayout from '../layouts/BaseLayout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import JobGrid from '../components/JobGrid.astro';
import IncomeCalculator from '../components/IncomeCalculator.astro';
import LanguageSwitcher from '../components/LanguageSwitcher.astro';
import ReviewsBlock from '../components/ReviewsBlock.astro';
import { CATEGORIES } from '../data/constants';
import jobsData from '../data/jobs.json';
import { getCompaniesFromJobs } from '../utils/companies';
import { getCitiesFromJobs, getCityNames } from '../utils/cities';

const cities = getCitiesFromJobs(jobsData);
const availableCities = getCityNames(jobsData);
const cityRouteMap = Object.fromEntries(cities.map((city) => [city.name, `/rabota-kurerom-${city.slug}/`]));
const companies = getCompaniesFromJobs(jobsData);
const totalVacancies = jobsData.length;
const topCities = [...cities]
	.sort((a, b) => b.vacancyCount - a.vacancyCount)
	.slice(0, 6)
	.map((city) => ({
		name: city.name,
		vacancyCount: city.vacancyCount,
		href: `/rabota-kurerom-${city.slug}/`,
	}));
const featuredCompanies = [...companies]
	.sort((a, b) => b.vacancyCount - a.vacancyCount)
	.slice(0, 6)
	.map((company) => ({
		name: company.name,
		href: company.href,
	}));
const featuredFormats = ['na-avto', 'peshkom', 'dlya-studentov', 'ezhednevnaya-oplata', 'samokat', 'kuper']
	.map((slug) => CATEGORIES.find((category) => category.slug === slug))
	.filter(Boolean)
	.map((category) => ({
		name: category!.name,
		href: `/rabota-kurerom-${category!.slug}/`,
	}));
const routeCards = [
	{
		title: 'Нужна подработка',
		text: 'Сразу перейти в гибкие графики, вечерние слоты и выходные смены.',
		href: '/rabota-kurerom-podrabotka/',
	},
	{
		title: 'Ищу работу с 16 лет',
		text: 'Открыть подборку для старта без авто и с мягкими входными условиями.',
		href: '/rabota-kurerom-16-let/',
	},
	{
		title: 'Есть автомобиль',
		text: 'Смотреть автоформаты с более высоким потенциалом дохода.',
		href: '/rabota-kurerom-na-avto/',
	},
	{
		title: 'Хочу выбрать бренд',
		text: 'Сначала сравнить компании, а потом уже вакансии внутри бренда.',
		href: '/companies/',
	},
];
const quickStartLinks = [
	{ label: 'Подработка', href: '/rabota-kurerom-podrabotka/' },
	{ label: 'С 16 лет', href: '/rabota-kurerom-16-let/' },
	{ label: 'На авто', href: '/rabota-kurerom-na-avto/' },
	{ label: 'Компании', href: '/companies/' },
	{ label: 'Сравнение', href: '/compare/' },
];
const heroFacts = [
	{ label: 'Вакансии', value: `${totalVacancies}` },
	{ label: 'Компании', value: `${companies.length}` },
	{ label: 'Города', value: `${cities.length}` },
	{ label: 'Сценарии', value: 'Авто, пешком, подработка' },
];
const faqItems = [
	{
		question: 'Со скольки лет можно работать курьером?',
		answer:
			'В большинстве компаний можно устроиться пешим курьером или велокурьером уже с 16 лет. Для работы на автомобиле обычно нужен возраст 18+ и права категории B.',
	},
	{
		question: 'Сколько реально зарабатывает курьер в 2026 году?',
		answer:
			'Доход зависит от города, транспорта, графика и компании. Пешие курьеры обычно зарабатывают меньше, а вело- и автоформаты дают более высокий потенциал дохода.',
	},
	{
		question: 'Нужна ли собственная термосумка?',
		answer:
			'Чаще всего нет: агрегаторы и сервисы доставки выдают экипировку и термосумку после подключения или короткого обучения.',
	},
];
const hubLinks = [
	{
		href: '/compare/',
		title: 'Сравнение вакансий',
		text: 'Быстро сравнить выплаты, транспорт и оформление перед откликом.',
	},
	{
		href: '/companies/',
		title: 'Компании',
		text: 'Сначала выбрать бренд, потом перейти в конкретные вакансии.',
	},
	{
		href: '/cities/',
		title: 'Города',
		text: 'Открыть городские хабы и локальные сценарии поиска работы.',
	},
	{
		href: '/blog/',
		title: 'Блог',
		text: 'Разобраться в доходе, форматах работы и скрытых условиях.',
	},
];
const discoveryCards = [
	...routeCards.map((card) => ({
		...card,
		kicker: 'Быстрый сценарий',
		variant: 'route',
	})),
	...hubLinks.map((card) => ({
		...card,
		kicker: 'Навигация',
		variant: 'hub',
	})),
];
const homepageTitle = 'Работа курьером: вакансии, компании и сравнение | Курьерок';
const homepageDescription = `Работа курьером в России: ${totalVacancies} вакансий, ${companies.length} компаний, города, сравнение и статьи перед откликом.`.slice(0, 170);
const pageSchemaGraph = [
	{
		'@type': 'CollectionPage',
		name: homepageTitle,
		description: homepageDescription,
		url: Astro.url.href,
		dateModified: new Date().toISOString(),
		mainEntity: {
			'@type': 'ItemList',
			itemListElement: hubLinks.map((link, index) => ({
				'@type': 'ListItem',
				position: index + 1,
				name: link.title,
				url: new URL(link.href, Astro.site).toString(),
			})),
		},
	},
	{
		'@type': 'Organization',
		name: 'Курьерок',
		url: Astro.site,
		description: 'Каталог вакансий курьеров, сравнений, компаний и городских хабов для быстрого выбора подходящей работы.',
	},
	{
		'@type': 'FAQPage',
		mainEntity: faqItems.map((item) => ({
			'@type': 'Question',
			name: item.question,
			acceptedAnswer: {
				'@type': 'Answer',
				text: item.answer,
			},
		})),
	},
];
---

<BaseLayout title={homepageTitle} description={homepageDescription} schemaGraph={pageSchemaGraph}>
	<Header />

	<main>
		<div class="home-shell">
			<section class="hero hero-home">
				<div class="home-hero-panel">
					<div class="home-hero-copy">
						<p class="home-kicker">Вакансии курьеров 2026</p>
						<h1>Работа курьером: вакансии, компании и сравнение условий</h1>
						<p class="home-hero-summary">
							На Курьерок можно быстро выбрать работу курьером по городу, формату доставки, бренду и частоте выплат.
							Сначала откройте сам список вакансий, а уже потом переходите в сравнение, компании и дополнительные хабы,
							если нужно сузить выбор.
						</p>
						<div class="home-hero-actions">
							<a href="#vacancies" class="home-hero-link">Смотреть вакансии</a>
							<a href="/compare/" class="home-hero-link home-hero-link-secondary">Сравнить условия</a>
						</div>

						<div class="home-hero-facts">
							{heroFacts.map((fact) => (
								<article class="home-hero-fact">
									<span>{fact.label}</span>
									<strong>{fact.value}</strong>
								</article>
							))}
						</div>
					</div>
				</div>

				<div class="hero-toolbar hero-toolbar-only">
					<div class="toolbar-top">
						<div class="city-selector-container" data-region-autoprompt="manual-only">
							<svg class="city-icon" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"></path><circle cx="12" cy="10" r="3"></circle></svg>
							<label class="sr-only" for="city-select">Выберите город</label>
							<select id="city-select" class="city-select">
								<option value="" data-t="location.all_cities">Все города</option>
								{availableCities.map((city) => (
									<option value={city}>{city}</option>
								))}
							</select>
						</div>
						<div class="hero-lang-switcher">
							<LanguageSwitcher />
						</div>
					</div>
					<a id="city-page-link" class="hero-city-link hidden" href="/" data-t="location.view_jobs">Все вакансии в этом городе</a>
				</div>
			</section>

			<section class="home-section home-vacancy-launch">
				<div class="section-heading">
					<h2>Сначала посмотрите вакансии</h2>
					<p>Лента начинается сразу ниже. Эти быстрые входы нужны только если вы уже точно знаете формат поиска и хотите сократить путь.</p>
				</div>
				<div class="home-vacancy-launch-bar">
					<a href="#vacancies" class="home-vacancy-launch-link">Перейти к ленте вакансий</a>
					<div class="home-quick-chip-row">
						{quickStartLinks.map((link) => (
							<a href={link.href} class="home-chip-link">{link.label}</a>
						))}
					</div>
				</div>
			</section>

			<JobGrid />

			<section class="home-section home-discovery-section">
				<div class="section-heading">
					<h2>Если хотите сузить выбор дальше</h2>
					<p>После просмотра ленты можно перейти в быстрые сценарии, компании, города и популярные подборки.</p>
				</div>
				<div class="home-navigation-grid">
					{discoveryCards.map((card) => (
						<a href={card.href} class={`home-navigation-card ${card.variant === 'route' ? 'home-navigation-card-route' : ''}`}>
							<span>{card.kicker}</span>
							<h3>{card.title}</h3>
							<p>{card.text}</p>
						</a>
					))}
				</div>
				<div class="home-surface-grid">
					<article class="home-surface-card">
						<h3>Популярные города</h3>
						<div class="home-chip-row">
							{topCities.map((city) => (
								<a href={city.href} class="home-chip-link">{city.name} · {city.vacancyCount}</a>
							))}
						</div>
					</article>

					<article class="home-surface-card">
						<h3>Популярные форматы</h3>
						<div class="home-chip-row">
							{featuredFormats.map((format) => (
								<a href={format.href} class="home-chip-link">{format.name}</a>
							))}
						</div>
					</article>

					<article class="home-surface-card">
						<h3>Сильные бренды</h3>
						<div class="home-chip-row">
							{featuredCompanies.map((company) => (
								<a href={company.href} class="home-chip-link">{company.name}</a>
							))}
						</div>
					</article>
				</div>
			</section>

			<IncomeCalculator />

			<ReviewsBlock />

			<section class="faq-section home-section">
				<div class="section-heading">
					<h2 data-t="faq.title">Частые вопросы</h2>
					<p data-t="faq.subtitle">Главное перед тем, как откликаться на вакансию.</p>
				</div>
				<div class="faq-item">
					<div class="faq-question">
						<span data-t="faq.q1">{faqItems[0].question}</span> <span>+</span>
					</div>
					<div class="faq-answer" data-t="faq.a1">{faqItems[0].answer}</div>
				</div>
				<div class="faq-item">
					<div class="faq-question">
						<span data-t="faq.q2">{faqItems[1].question}</span> <span>+</span>
					</div>
					<div class="faq-answer" data-t="faq.a2">{faqItems[1].answer}</div>
				</div>
				<div class="faq-item">
					<div class="faq-question">
						<span data-t="faq.q3">{faqItems[2].question}</span> <span>+</span>
					</div>
					<div class="faq-answer" data-t="faq.a3">{faqItems[2].answer}</div>
				</div>
			</section>
		</div>
	</main>

	<Footer />
</BaseLayout>

<script is:inline define:vars={{ availableCities, cityRouteMap }}>
	document.addEventListener('DOMContentLoaded', () => {
		const STORAGE_KEY = 'kurieros-selected-city';
		const citySelect = document.getElementById('city-select');
		const cityPageLink = document.getElementById('city-page-link');
		const citySelectorContainer = citySelect?.closest('.city-selector-container');
		const shouldAutoPromptRegion = citySelectorContainer?.dataset.regionAutoprompt !== 'manual-only';

		function updateCityLink(city) {
			const href = cityRouteMap[city];
			if (!city || !href) {
				cityPageLink.classList.add('hidden');
				cityPageLink.removeAttribute('href');
				return;
			}

			cityPageLink.href = href;
			cityPageLink.classList.remove('hidden');
		}

		function getTranslation(key, defaultText) {
			const lang = window.kurieros_i18n?.currentLang || 'ru';
			const dict = window.translations ? (window.translations[lang] || window.translations['ru']) : null;
			if (!dict) return defaultText;
			return key.split('.').reduce((prev, curr) => prev && prev[curr], dict) || defaultText;
		}

		function applyCity(city, source = 'manual') {
			const normalizedCity = availableCities.includes(city) ? city : '';
			citySelect.value = normalizedCity;
			updateCityLink(normalizedCity);

			if (normalizedCity) {
				localStorage.setItem(STORAGE_KEY, normalizedCity);
			} else {
				localStorage.removeItem(STORAGE_KEY);
			}

			window.dispatchEvent(new CustomEvent('kurieros:city-selected', {
				detail: { city: normalizedCity, source }
			}));
		}

		function normalizeDetectedCity(name) {
			if (!name) return '';
			const directMatch = availableCities.find((city) => city.toLowerCase() === name.toLowerCase());
			if (directMatch) return directMatch;
			const trimmed = name.replace(/^город\s+/i, '').trim();
			return availableCities.find((city) => city.toLowerCase() === trimmed.toLowerCase()) || '';
		}

		async function getIpData() {
			if (window.kurieros_user) {
				return window.kurieros_user;
			}

			return new Promise((resolve) => {
				const timeout = setTimeout(() => resolve(null), 4000);
				window.addEventListener('kurieros:region-detected', (event) => {
					clearTimeout(timeout);
					resolve(event.detail);
				}, { once: true });
			});
		}

		async function detectCityByIP() {
			const ipData = await getIpData();
			if (!ipData) return;

			// VPN detected — country is not Russia
			if (ipData.country && ipData.country !== 'RU') {
				// Change "Все города" to "Выбор города"
				const allCitiesOption = citySelect.querySelector('option[value=""]');
				if (allCitiesOption) {
					const selectText = getTranslation('location.select_city', 'Выбор города');
					allCitiesOption.textContent = selectText;
					allCitiesOption.setAttribute('data-t', 'location.select_city');
				}

				if (shouldAutoPromptRegion) {
					citySelect.focus();
					window.dispatchEvent(new CustomEvent('kurieros:manual-city-required'));
				}
				return;
			}

			const detectedCity = normalizeDetectedCity(ipData.city);
			if (detectedCity) {
				applyCity(detectedCity, 'ip');
				return;
			}

			// City not recognized — let user pick manually
			citySelect.focus();
		}

		const savedCity = localStorage.getItem(STORAGE_KEY);
		if (savedCity && availableCities.includes(savedCity)) {
			applyCity(savedCity, 'saved');
		} else {
			detectCityByIP();
		}

		citySelect?.addEventListener('change', (event) => {
			applyCity(event.target.value, 'manual');
		});

		document.querySelectorAll('.faq-item').forEach((item) => {
			item.querySelector('.faq-question')?.addEventListener('click', () => {
				item.classList.toggle('active');
			});
		});
	});
</script>

<style>
	/* --- Layout --- */
	main {
		max-width: 1680px;
	}
	.home-shell {
		max-width: 1560px;
		margin: 0 auto;
		padding: 1.25rem 1.5rem 4rem;
	}
	.hero-home {
		display: grid;
		gap: 0.6rem;
		margin-bottom: 1rem;
	}
	.home-hero-panel {
		display: grid;
		grid-template-columns: 1fr;
		gap: 1.35rem;
		padding: 1.75rem;
		border: 1px solid #ebe5dc;
		border-radius: 1.8rem;
		background:
			radial-gradient(circle at top right, rgba(240, 203, 120, 0.22), transparent 18rem),
			linear-gradient(180deg, #fffefb 0%, #f8f1e7 100%);
		box-shadow: 0 14px 36px rgba(60, 41, 20, 0.08);
	}
	.home-hero-copy {
		display: grid;
		gap: 0.9rem;
		align-content: start;
		max-width: 74rem;
	}
	.home-kicker {
		margin: 0;
		font-size: 0.78rem;
		font-weight: 900;
		letter-spacing: 0.14em;
		text-transform: uppercase;
		color: #8b5e34;
	}
	.home-hero-copy h1 {
		margin: 0;
		font-size: clamp(2.2rem, 3.8vw, 4.2rem);
		line-height: 0.94;
		letter-spacing: -0.06em;
		max-width: 15ch;
		text-wrap: balance;
	}
	.home-hero-summary {
		max-width: 62ch;
		margin: 0;
		color: #6f6257;
		font-size: clamp(1rem, 1vw + 0.9rem, 1.18rem);
		line-height: 1.72;
	}
	.home-hero-actions {
		display: flex;
		flex-wrap: wrap;
		gap: 0.8rem;
	}
	.home-hero-link {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		min-height: 3rem;
		padding: 0.8rem 1.2rem;
		border-radius: 999px;
		background: #1f2937;
		border: 1px solid #1f2937;
		color: white;
		text-decoration: none;
		font-weight: 800;
		transition: var(--transition);
	}
	.home-hero-link-secondary {
		background: rgba(255, 255, 255, 0.86);
		color: #2b2622;
		border-color: #d9d0c3;
	}
	.home-hero-link:hover {
		transform: translateY(-1px);
		box-shadow: 0 10px 24px rgba(60, 41, 20, 0.08);
	}
	.home-hero-facts {
		display: grid;
		grid-template-columns: repeat(4, minmax(0, 1fr));
		gap: 0.75rem;
	}
	.home-hero-fact {
		display: grid;
		gap: 0.2rem;
		min-height: auto;
		padding: 0.8rem 0.9rem;
		border-radius: 1.2rem;
		border: 1px solid #e8ddcf;
		background: rgba(255, 255, 255, 0.82);
		box-shadow: 0 6px 18px rgba(60, 41, 20, 0.05);
	}
	.home-hero-fact span {
		font-size: 0.72rem;
		font-weight: 900;
		letter-spacing: 0.1em;
		text-transform: uppercase;
		color: #8b7d70;
	}
	.home-hero-fact strong {
		align-self: start;
		font-size: 1.02rem;
		line-height: 1.3;
		color: #2b2622;
	}
	.home-section {
		padding: 2.25rem;
		margin: 1.5rem auto;
		max-width: 1560px;
		width: 100%;
		background: #fffdf8;
		border: 1px solid #ebe5dc;
		border-radius: 1.6rem;
		box-shadow: 0 8px 24px rgba(60, 41, 20, 0.05);
	}
	.home-vacancy-launch {
		margin-top: 0.5rem;
	}
	.home-vacancy-launch-bar {
		display: grid;
		grid-template-columns: minmax(240px, 0.44fr) minmax(0, 1fr);
		gap: 1rem;
		align-items: start;
	}
	.home-vacancy-launch-link {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		min-height: 3.1rem;
		padding: 0.85rem 1.2rem;
		border-radius: 999px;
		background: #1f2937;
		border: 1px solid #1f2937;
		color: white;
		text-decoration: none;
		font-weight: 800;
		transition: var(--transition);
	}
	.home-vacancy-launch-link:hover {
		transform: translateY(-1px);
		box-shadow: 0 10px 24px rgba(15, 23, 42, 0.08);
	}
	.home-quick-chip-row {
		display: flex;
		flex-wrap: wrap;
		gap: 0.7rem;
		align-items: center;
	}
	.home-discovery-section {
		display: grid;
		gap: 1.5rem;
	}

	/* --- Hero Toolbar --- */
	.hero-toolbar {
		display: flex;
		flex-direction: column;
		gap: 0.75rem;
		margin-top: 2rem;
		margin-bottom: 2rem;
		width: 100%;
		max-width: 1000px;
		margin-left: auto;
		margin-right: auto;
	}
	.hero-toolbar-only {
		padding: 0;
	}
	.toolbar-top {
		display: flex;
		gap: 0.75rem;
		width: 100%;
		justify-content: center;
	}
	.city-selector-container {
		display: flex;
		align-items: center;
		padding: 0 1.25rem;
		/* Removing padding-right: 2.75rem; as we'll use absolute pos for arrow */
		background: #f7f2ea;
		border: 1px solid #ebe5dc;
		font-size: 1.4rem;
		box-shadow: 0 6px 16px rgba(60, 41, 20, 0.12);
		flex: 1.3;
		height: 4.8rem;
		box-sizing: border-box;
		border-radius: 999px;
		cursor: pointer;
		position: relative;
		transition: var(--transition);
	}
	.city-selector-container:hover {
		border-color: #d7cdbe;
		background: #fcfaf7;
		transform: translateY(-2px);
		box-shadow: 0 8px 20px rgba(0,0,0,0.06);
	}
	.city-selector-container::after {
		content: '▼';
		position: absolute;
		right: 1.5rem;
		font-size: 0.75rem;
		color: #8b7d70;
		pointer-events: none;
	}
	.hero-lang-switcher {
		flex: 1;
	}

	/* Inner Lang Override */
	.hero-lang-switcher :global(.lang-toggle) {
		height: 4.8rem !important;
		width: 100% !important;
		padding: 0 1.25rem !important;
		border-radius: 999px !important;
		font-size: 1.4rem !important;
		font-weight: 800 !important;
		background: #f7f2ea !important;
		border: 1px solid #ebe5dc !important;
		justify-content: center !important;
		box-shadow: 0 6px 16px rgba(60, 41, 20, 0.12) !important;
		box-sizing: border-box !important;
	}
	.hero-lang-switcher :global(.lang-toggle span) {
		font-weight: 800 !important;
	}
	.hero-lang-switcher :global(.flag-icon) {
		font-size: 1.5rem !important;
	}

	/* --- City Select & Icon --- */
	.city-icon {
		position: absolute;
		left: 1.25rem;
		color: #8b7d70;
		flex-shrink: 0;
		width: 20px;
		height: 20px;
		pointer-events: none;
		z-index: 2;
	}
	.city-select {
		width: 100%;
		border: none;
		background: transparent;
		appearance: none;
		font: inherit;
		font-size: inherit !important;
		font-weight: 800 !important;
		color: #2b2622;
		padding: 0 2.5rem;
		outline: none;
		text-align: center;
		text-align-last: center;
		cursor: pointer;
	}

	/* --- Hero CTA Button --- */
	.hero-city-link {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		margin-top: 0.25rem;
		padding: 0 2.5rem;
		height: 4.8rem;
		box-sizing: border-box;
		border-radius: 999px;
		border: 1px solid var(--text-main);
		background: var(--text-main);
		color: #fff;
		text-decoration: none;
		font-size: clamp(1.1rem, 4.5vw, 1.4rem);
		line-height: 1.2;
		transition: var(--transition);
		width: 100%;
	}
	.hero-city-link:hover {
		transform: translateY(-1px);
		box-shadow: 0 10px 24px rgba(15, 23, 42, 0.08);
	}

	/* --- Content Sections --- */
	.section-heading {
		margin-bottom: 1.1rem;
	}
	.section-heading h2 {
		margin-bottom: 0.35rem;
		letter-spacing: -0.03em;
	}
	.section-heading p {
		color: #7c6f64;
	}
	:global(.reviews-section),
	:global(.faq-section) {
		border: none !important;
		background: transparent !important;
		box-shadow: none !important;
		padding: 0 !important;
	}
	:global(.review-card),
	:global(.faq-item) {
		background: #fffdf8 !important;
		border: 1px solid #ebe5dc !important;
		border-radius: 1.25rem !important;
		box-shadow: 0 4px 12px rgba(60, 41, 20, 0.03) !important;
	}
	.home-navigation-grid {
		display: grid;
		grid-template-columns: repeat(4, minmax(0, 1fr));
		gap: 1rem;
	}
	.home-navigation-card {
		display: grid;
		gap: 0.55rem;
		padding: 1.25rem;
		border-radius: 1.25rem;
		border: 1px solid #ebe5dc;
		background: linear-gradient(180deg, #fffefb 0%, #f8f1e7 100%);
		box-shadow: 0 6px 18px rgba(60, 41, 20, 0.05);
		text-decoration: none;
		color: #2b2622;
		transition: var(--transition);
	}
	.home-navigation-card-route {
		background:
			radial-gradient(circle at top right, rgba(99, 102, 241, 0.1), transparent 10rem),
			linear-gradient(180deg, #fffefb 0%, #eef3ff 100%);
	}
	.home-navigation-card:hover {
		transform: translateY(-2px);
		box-shadow: 0 10px 24px rgba(60, 41, 20, 0.08);
	}
	.home-navigation-card span,
	.home-navigation-card h3,
	.home-navigation-card p {
		margin: 0;
	}
	.home-navigation-card span {
		font-size: 0.72rem;
		font-weight: 900;
		letter-spacing: 0.1em;
		text-transform: uppercase;
		color: #8b7d70;
	}
	.home-navigation-card h3 {
		font-size: 1rem;
	}
	.home-navigation-card p {
		color: #7c6f64;
		line-height: 1.6;
	}
	.home-surface-grid {
		display: grid;
		grid-template-columns: repeat(3, minmax(0, 1fr));
		gap: 1rem;
	}
	.home-surface-card {
		padding: 1.35rem;
		border-radius: 1.25rem;
		border: 1px solid #ebe5dc;
		background: linear-gradient(180deg, #fffefb 0%, #f8f1e7 100%);
		box-shadow: 0 6px 18px rgba(60, 41, 20, 0.05);
	}
	.home-surface-card h3 {
		margin: 0 0 0.8rem;
		font-size: 1rem;
	}
	.home-chip-row {
		display: flex;
		flex-wrap: wrap;
		gap: 0.7rem;
	}
	.home-chip-link {
		display: inline-flex;
		align-items: center;
		min-height: 2.35rem;
		padding: 0.55rem 0.85rem;
		border-radius: 999px;
		border: 1px solid #ddd3c5;
		background: rgba(255, 255, 255, 0.86);
		color: #2b2622;
		font-weight: 700;
		text-decoration: none;
		transition: var(--transition);
	}
	.home-chip-link:hover {
		transform: translateY(-1px);
		box-shadow: 0 8px 18px rgba(60, 41, 20, 0.07);
	}

	/* --- Utilities --- */
	.sr-only {
		position: absolute;
		width: 1px;
		height: 1px;
		padding: 0;
		margin: -1px;
		overflow: hidden;
		clip: rect(0, 0, 0, 0);
		white-space: nowrap;
		border: 0;
	}
	.hidden {
		display: none;
	}

	/* --- Mobile --- */
	@media (max-width: 768px) {
		.home-shell {
			padding-top: 0.75rem;
		}
		.home-hero-panel {
			grid-template-columns: 1fr;
			padding: 1rem;
			gap: 0.75rem;
		}
		.home-hero-copy {
			gap: 0.85rem;
		}
		.home-hero-summary {
			font-size: 1rem;
			line-height: 1.6;
		}
		.home-hero-facts {
			display: none;
		}
		.home-section {
			padding: 1.2rem;
			border-radius: 1.25rem;
		}
		.hero-toolbar {
			margin-top: 1rem;
			margin-bottom: 1rem;
		}
		.home-vacancy-launch-bar {
			grid-template-columns: 1fr;
		}
		.toolbar-top {
			flex-direction: row;
			gap: 0.5rem;
			width: 100%;
		}
		.city-selector-container {
			flex: 2;
			min-width: 0;
			height: 4.8rem;
			padding: 0 0.5rem !important;
			padding-right: 1.5rem !important;
		}
		.city-icon {
			left: 0.9rem;
			width: 18px;
			height: 18px;
		}
		.hero-lang-switcher {
			flex: 1;
			min-width: 0;
		}
		.hero-lang-switcher :global(.lang-toggle) {
			height: 4.8rem !important;
			width: 100% !important;
			padding: 0 0.5rem !important;
			font-size: 1.1rem !important;
			justify-content: center;
		}
		.city-select {
			padding: 0 1.25rem 0 1.9rem;
			font-size: 1.1rem !important;
			text-align: left;
			text-align-last: left;
		}
		.home-navigation-grid,
		.home-surface-grid {
			grid-template-columns: repeat(2, minmax(0, 1fr));
		}
	}
	@media (max-width: 600px) {
		.home-hero-copy h1 {
			max-width: none;
			font-size: clamp(2rem, 11vw, 3.6rem);
			line-height: 0.98;
		}
		.home-navigation-grid,
		.home-surface-grid {
			grid-template-columns: 1fr;
		}
	}
</style>
