// Shared «main» nav label (rendered as the dropdown root of the mobile
// bottom-nav). Merged into `translations.nav.main` at index assembly time.
//
// Russian-only per PR #131 policy — shell UI does not translate.

export const navMainTranslations = 'Главная';
